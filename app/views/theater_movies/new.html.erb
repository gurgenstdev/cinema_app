<%= form_for @theaterMovies, url: theater_theater_movies_path(@theater.id), method: :post do %>
  <%= select_tag 'movie_id', options_for_select(@movie.collect{ |u| [u.name, u.id] })%>
  <%= submit_tag value="submit" %>
<% end %>