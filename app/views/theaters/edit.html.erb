<h1>Edit <%= @theater.name %></h1>
<%= link_to("<<Back to the list", theaters_path) %>

<%= form_for(@theater) do |t| %>
  <p>
    Name of the theater: <br>
    <%= t.text_field :name %>
  </p>
  <p>
    <%= t.submit %>
  </p>
<% end %>